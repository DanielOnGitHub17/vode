asgiref==3.10.0
Django==5.2.7
sqlparse==0.5.3
tzdata==2025.2

python-dotenv==1.0.0
google-generativeai==0.8.5
requests==2.31.0

# Production dependencies (Heroku will install these)
gunicorn==21.2.0
whitenoise==6.6.0
dj-database-url==2.1.0

# PostgreSQL adapter - only needed on Heroku, not for local SQLite development
# Heroku's Python buildpack will install this properly
psycopg2-binary==2.9.9; platform_system != "Windows"
